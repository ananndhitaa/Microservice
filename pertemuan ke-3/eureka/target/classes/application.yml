spring:
  application:
    name: eureka
server:
  port: 8761
  client:
    register-with-eureka: false
    fetch-registry: false  
  logging:
    level:
      com.netflix.discovery: OFF
      org.netflix.eureka: OFF  